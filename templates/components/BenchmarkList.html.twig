<div {{ attributes }}>
    <div data-loading="delay|show">
        <div class="stats-card__container">
            <span class="spinner">⏳</span> Chargement de la liste des benchmarks...
        </div>
    </div>

    {% if this.benchmarks %}
        <div data-loading="hide" class="benchmark-list">
            {% for benchmark in this.benchmarks %}
                <twig:BenchmarkCard
                    benchmarkId="{{ benchmark.benchId }}"
                    benchmarkName="{{ benchmark.name }}"
                    data-benchmark-filter-target="card"
                />
            {% endfor %}
        </div>
    {% endif %}
</div>
