# Benchmark Profiles for Different Use Cases

# Quick testing (development)
# - Fast feedback, less precision
# BENCHMARK_WARMUP_ITERATIONS=3
# BENCHMARK_INNER_ITERATIONS=10

# Balanced (default)
# - Good balance between speed and precision
# BENCHMARK_WARMUP_ITERATIONS=10
# BENCHMARK_INNER_ITERATIONS=100

# Precise measurements
# - Higher precision, slower execution
# BENCHMARK_WARMUP_ITERATIONS=20
# BENCHMARK_INNER_ITERATIONS=500

# Scientific/Research
# - Maximum precision for publication
# BENCHMARK_WARMUP_ITERATIONS=50
# BENCHMARK_INNER_ITERATIONS=1000

# Heavy benchmarks (mb_*, preg_*, etc.)
# - Reduced iterations for expensive operations
# BENCHMARK_WARMUP_ITERATIONS=5
# BENCHMARK_INNER_ITERATIONS=50