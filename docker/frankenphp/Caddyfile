{
    # Global options
    auto_https off
    order php_server before file_server
}

:80 {
    # Set the root directory
    root * /app/public
    
    # Enable compression
    encode gzip zstd
    
    # Log to stdout
    log {
        output stdout
        format json
        level INFO
    }
    
    # Handle PHP files (worker mode disabled for now due to PHP 8.3/8.4 compatibility issues)
    php_server
}
